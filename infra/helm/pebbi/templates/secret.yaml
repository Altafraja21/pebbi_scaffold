apiVersion: v1
kind: Secret
metadata:
name: pebbi-secrets
type: Opaque
stringData:
OPENAI_API_KEY: "{{ .Values.openai_api_key | default "REPLACE_ME" }}"
DATABASE_URL: "{{ .Values.database_url | default "REPLACE_ME" }}"